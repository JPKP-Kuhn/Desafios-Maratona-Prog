n, m = map(int, input().split())

while n != 0 and m != 0:

    mat = [0] * n
    for i in range(n):
        mat[i] = list(map(int, input().split()))

    count = 0 # Características pedidas

    # Ninguém resolveu todos os problemas
    for i in range(n):
        for j in range(m):
            pass

    n, m = map(int, input().split())
