#include <bits/stdc++.h>
#include <cstdio>
using namespace std;

int main(){
  for(;;){
    int n,m;
    cin >> n >> m;
    
    int mat[n][m];
    
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            cin >> mat[i][j];
        }
    }

    if (EOF) break;
  }
  
  return 0;
}

